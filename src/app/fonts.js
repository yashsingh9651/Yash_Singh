import { Tilt_Prism,Phudu } from 'next/font/google';

export const tiltPrism = Tilt_Prism({
  subsets: ['latin'],
  weight:'400'
});
export const phudu = Phudu({
  subsets: ['latin'],
  weight:['400','600','700','800']
});